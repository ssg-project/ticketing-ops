apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticketing.fullname" . }}
  labels:
    {{- include "ticketing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ticketing.selectorLabels" . | nindent 4 }}
