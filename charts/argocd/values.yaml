argo-cd:
  server:
    service:
      type: ClusterIP
    ingress:
      enabled: true
      ingressClassName: nginx
      hosts:
        - host: argocd.gmmt.store
          paths:
            - path: /
              pathType: Prefix