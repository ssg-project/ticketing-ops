apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
data:
  # Kafka 설정
  KAFKA_BROKER_URL: {{ .Values.secret.kafka.brokerUrl | b64enc }}
  KAFKA_TOPIC: {{ .Values.secret.kafka.topic | b64enc }}
  KAFKA_GROUP_ID: {{ .Values.secreot.kafka.groupId | b64enc }}